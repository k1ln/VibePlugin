# Compander — tape-NR character

Models target: **Effects #98 — Dolby A/SR + Dolby HX** (companding / noise-reduction character).

A companding compress→expand with high-frequency pre-emphasis — the tape-NR sheen and gentle
"breathing", used as a tone/dynamics colour rather than true noise reduction. Controls: Squeeze
(compand depth), HF (high-shelf emphasis), Tone (tilt), Output, Mix. No host imports, no alloc in
process(). wasm-runner PASS (all 5 params reactive); GUI render-checked (0 console errors).
